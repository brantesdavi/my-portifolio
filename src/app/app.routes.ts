import { Routes } from '@angular/router';
import { ContactSegmentComponent } from './components/contact-segment/contact-segment.component';

export const routes: Routes = [
    { path: 'contact', component: ContactSegmentComponent }
];
